type NavLinks = {
  title: string;
  slug: string;
};

const navLinks: NavLinks[] = [
  {
    title: "Recursos",
    slug: "/recursos",
  },
  {
    title: "Depoimentos",
    slug: "/depoimentos",
  },
];

const NavLink = () => {
  return <div>NavLink</div>;
};

export default NavLink;
